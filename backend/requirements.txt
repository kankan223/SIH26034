fastapi==0.115.6
uvicorn[standard]==0.30.6
asyncpg==0.29.0
sqlalchemy==2.0.36
alembic==1.13.2
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
slowapi==0.1.9
httpx==0.27.2

# CV / OCR
opencv-python-headless==4.10.0.84
ultralytics==8.3.14
onnxruntime==1.29.0
paddleocr==2.9.1  # multilingual OCR (English + Hindi per prd.md §11.2; pinned for Python 3.12 runtime)
paddlepaddle>=3.3.0,<4.0.0  # paddleocr 2.9.1 requires paddlepaddle>=3.3.0 (3.3.x runtime)

# ML
scikit-learn==1.5.2
joblib==1.4.2

# Storage
boto3==1.34.102

# PDF / DOCX
weasyprint==62.3
python-docx==1.1.2

# Utils — pinned to patched versions per pip-audit PYSEC findings
pillow==12.3.0  # patched per PYSEC-2026-2253/2255/2256/3451/3453/3454/3493/3494/3495/3496
starlette>=0.40.0,<0.42.0  # pinned to compatible range for fastapi==0.115.6
ecdsa==0.19.2  # patched per PYSEC-2026-1325
